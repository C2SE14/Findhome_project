const initState = {
  isLoggedIn: false,
  token: null,
};

const authReducer = (state = initState, acion) => {
  switch (acion.type) {
    default:
      return state;
  }
};

export default authReducer;
